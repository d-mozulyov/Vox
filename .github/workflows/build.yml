name: Build and Release

# This workflow uses a pre-built Docker image for faster builds.
# Official image: ghcr.io/d-mozulyov/vox-builder:latest
#
# The image is updated manually when dependencies change (rare).
# See docs/DOCKER_BUILD.md for instructions on updating the image.
#
# For forks: You can use the official image or build your own.
# To use your own image, set the BUILDER_IMAGE repository variable:
# Settings → Secrets and variables → Actions → Variables → New variable
# Name: BUILDER_IMAGE, Value: ghcr.io/YOUR_USERNAME/vox-builder:latest

on:
  workflow_dispatch:
  push:
    branches: [ main, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    container:
      image: ${{ vars.BUILDER_IMAGE || 'ghcr.io/d-mozulyov/vox-builder:latest' }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          /go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Run tests
      run: |
        # Start virtual frame buffer for headless X11
        Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
        export DISPLAY=:99.0
        sleep 1
        go test ./...

  build:
    name: Build
    needs: test
    runs-on: ubuntu-latest
    container:
      image: ${{ vars.BUILDER_IMAGE || 'ghcr.io/d-mozulyov/vox-builder:latest' }}
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          /go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Build for all platforms
      run: |
        # Configure Git to trust the workspace directory
        git config --global --add safe.directory "$GITHUB_WORKSPACE"

        # Extract version from tag (remove 'v' prefix)
        VERSION="${GITHUB_REF_NAME#v}"
        BUILD_TIME=$(date -u '+%Y-%m-%d_%H:%M:%S')
        LDFLAGS="-s -w -X main.Version=${VERSION} -X main.BuildTime=${BUILD_TIME}"

        # Create output directory
        mkdir -p dist

        echo "Building Vox version ${VERSION}"
        echo "================================"

        # Build Windows binaries
        echo "Building for Windows amd64..."
        GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -ldflags "${LDFLAGS}" -o "dist/vox-windows-amd64.exe" ./cmd/vox

        echo "Building for Windows arm64..."
        GOOS=windows GOARCH=arm64 CGO_ENABLED=0 go build -ldflags "${LDFLAGS}" -o "dist/vox-windows-arm64.exe" ./cmd/vox

        # Build Linux binaries with musl for maximum compatibility
        echo "Building for Linux amd64 (with musl)..."
        GOOS=linux GOARCH=amd64 CGO_ENABLED=1 CC=gcc go build -ldflags "${LDFLAGS} -linkmode external -extldflags '-static'" -o "dist/vox-linux-amd64" ./cmd/vox

        echo "Building for Linux arm64 (with musl)..."
        GOOS=linux GOARCH=arm64 CGO_ENABLED=1 CC=aarch64-linux-musl-gcc go build -ldflags "${LDFLAGS} -linkmode external -extldflags '-static'" -o "dist/vox-linux-arm64" ./cmd/vox

        # Build macOS binaries
        echo "Building for macOS amd64..."
        GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 go build -ldflags "${LDFLAGS}" -o "dist/vox-darwin-amd64" ./cmd/vox

        echo "Building for macOS arm64..."
        GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -ldflags "${LDFLAGS}" -o "dist/vox-darwin-arm64" ./cmd/vox

        echo "================================"
        echo "Build complete!"
        ls -lh dist/
      shell: bash

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: vox-binaries
        path: dist/*

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: vox-binaries
        path: dist

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
